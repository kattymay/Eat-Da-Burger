
<h1>Eat-Da-Burger!</h1>

<h2>Burgers that are ready to eat!</h2>

<ul class="orders">
  {{#each burgers}}
  {{#unless devoured}}
  <li>
    {{burger_name}}
    <button class="button eat" data-id="{{id}}" data-state="{{devoured}}">Eat-da-burger!</button>
  </li>
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul class="devoured">
  {{#each burgers}}
  {{#if devoured}}
  <li>
    {{burger_name}}
  </li>
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>


  <button type="submit">Add Burger</button>
</form>